// 3 listener event (mouse down(begin the pth), mouse move(draw), mouse up(dataURL), coordinate(e.) moveto, lineto)

console.log("sanity");
const canvas = document.getElementById("signature-canvas");

// canvas.toDataURL()

// value="data:image: hfgvbiuhjfgcvfyg"

// HINT:
// listen for 3 events:
//      mouse down (beggin path)
//      mouse move (stroke)
//      mouse up (canvas.toDataURL())
